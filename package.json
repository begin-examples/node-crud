{
  "name": "begin-app",
  "version": "0.0.0",
  "description": "Begin basic CRUD app",
  "scripts": {
    "lint": "eslint src --ignore-pattern node_modules --fix",
    "test": "cross-env NODE_ENV=testing tape test/*-test.js | tap-spec",
    "start": "cross-env NODE_ENV=testing BEGIN_DATA_TABLE_NAME=begin-app-staging-data npx sandbox"
  },
  "devDependencies": {
    "@architect/sandbox": "latest",
    "@begin/data": "latest",
    "cross-env": "^5.2.1",
    "eslint": "^6.3.0",
    "tap-spec": "^5.0.0",
    "tape": "^4.11.0",
    "tiny-json-http": "^7.1.2"
  }
}
